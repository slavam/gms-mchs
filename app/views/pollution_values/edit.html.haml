%h1= "Изменить оптическую плотность"

%div{ class: "row"}
  %div{ class: "col-md-6 col-md-offset-3"}
    = form_for(@pollution_value) do |p_v| 
      //   = render 'shared/error_messages'
      = p_v.label :value, "Плотность"
      = p_v.text_field :value, class: 'form-control'

      = p_v.submit "Сохранить", class: "btn btn-primary"