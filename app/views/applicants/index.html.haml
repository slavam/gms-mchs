%div{ class: "container"}
  %h1  Телеграммы в буфере
  %table{class: "table table-hover"}
    %thead
      %tr
        %th{:style => "width: 200px"} Дата загрузки
        %th Телеграмма
        %th= ""
        %th= ""
    %tbody
      - @applicants.each do |applicant| 
        %tr
          %td= applicant.created_at.to_s
          %td= applicant.telegram
          %td= link_to 'Изменить', edit_applicant_path(applicant.id)
          %td= link_to 'Удалить', applicant, method: :delete, data: { confirm: 'Уверены?' }
  = link_to "Добавить телеграмму", new_applicant_path